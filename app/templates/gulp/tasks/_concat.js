var gulp = require('gulp');
var concat = require('gulp-concat');

gulp.task('concat', function() {
	/* Insert the scripts you need to be concatenated here
	* Example:
  	* gulp.src(['./src/js/file2.js', './src/js/file1.js'])
    * .pipe(concat('all.min.js'))
    * .pipe(gulp.dest('./build/js'))
    * You can also use wildcard: gulp.src(['./src/js/*.js'])
    */
});