var gulp       = require('gulp');

gulp.task('watch', function(){
	/**
	* Include the tasks you want to execute
	* Examples:
	* gulp.watch('src/js/**', ['jshint', 'jsmin']);
	* gulp.watch('src/sass/**', ['sass']);
	* gulp.watch('src/img/**', ['imagemin']);
	*/
});
